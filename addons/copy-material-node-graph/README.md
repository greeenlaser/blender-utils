# Copy material node graph

This is a full blender addon and can be added to Blender 4.5.x (and probably newer and older versions, but untested).

This utility adds a right-click context while in Shader Editor with the `Copy Material Node Graph` button that copies all active nodes and their connections to your clipboard.
